meta {
  name: Removes the Invoice resource.
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/invoices/:id
  body: none
  auth: none
}

params:path {
  id: 
}

script:post-response {
  if (res.status === 404) {
  }
}
