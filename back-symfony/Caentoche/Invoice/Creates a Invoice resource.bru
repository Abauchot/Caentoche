meta {
  name: Creates a Invoice resource.
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/invoices
  body: none
  auth: none
}

script:post-response {
  if (res.status === 201) {
  }
  if (res.status === 400) {
  }
  if (res.status === 422) {
  }
}
