meta {
  name: Removes the Tarif resource.
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/tarifs/:id
  body: none
  auth: none
}

params:path {
  id: 
}

script:post-response {
  if (res.status === 404) {
  }
}
