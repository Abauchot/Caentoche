meta {
  name: Removes the Item resource.
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/items/:id
  body: none
  auth: none
}

params:path {
  id: 
}

script:post-response {
  if (res.status === 404) {
  }
}
