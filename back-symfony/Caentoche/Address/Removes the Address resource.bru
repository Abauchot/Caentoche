meta {
  name: Removes the Address resource.
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/addresses/:id
  body: none
  auth: none
}

params:path {
  id: 
}

script:post-response {
  if (res.status === 404) {
  }
}
