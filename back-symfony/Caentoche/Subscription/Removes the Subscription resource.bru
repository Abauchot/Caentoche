meta {
  name: Removes the Subscription resource.
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/subscriptions/:id
  body: none
  auth: none
}

params:path {
  id: 
}

script:post-response {
  if (res.status === 404) {
  }
}
