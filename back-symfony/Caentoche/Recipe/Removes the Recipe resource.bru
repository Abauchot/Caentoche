meta {
  name: Removes the Recipe resource.
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/recipes/:id
  body: none
  auth: none
}

params:path {
  id: 
}

script:post-response {
  if (res.status === 404) {
  }
}
