meta {
  name: Creates a Recipe resource.
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/recipes
  body: none
  auth: none
}

script:post-response {
  if (res.status === 201) {
  }
  if (res.status === 400) {
  }
  if (res.status === 422) {
  }
}
