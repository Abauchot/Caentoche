meta {
  name: Removes the Ingredient resource.
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/ingredients/:id
  body: none
  auth: none
}

params:path {
  id: 
}

script:post-response {
  if (res.status === 404) {
  }
}
