meta {
  name: Removes the Payment resource.
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/payments/:id
  body: none
  auth: none
}

params:path {
  id: 
}

script:post-response {
  if (res.status === 404) {
  }
}
