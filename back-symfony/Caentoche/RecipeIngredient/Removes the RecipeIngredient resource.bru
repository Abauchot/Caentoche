meta {
  name: Removes the RecipeIngredient resource.
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/recipe_ingredients/:id
  body: none
  auth: none
}

params:path {
  id: 
}

script:post-response {
  if (res.status === 404) {
  }
}
